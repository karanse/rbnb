<div class="d-flex flex-column align-items-center">
  <h2 class="mb-4">All booking requests for your properties</h2>
  <div class="offer-card-container d-flex align-items-center">
    <% @offers.each do |offer| %>
      <%= render "shared/offer-card", offer: offer %>
    <% end %>
  </div>
</div>
